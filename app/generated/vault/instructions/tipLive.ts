/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const TIP_LIVE_DISCRIMINATOR = new Uint8Array([
  136, 165, 185, 13, 138, 97, 76, 39,
]);

export function getTipLiveDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TIP_LIVE_DISCRIMINATOR);
}

export type TipLiveInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountFan extends string | AccountMeta<string> = string,
  TAccountLive extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFan extends string
        ? WritableSignerAccount<TAccountFan> & AccountSignerMeta<TAccountFan>
        : TAccountFan,
      TAccountLive extends string
        ? WritableAccount<TAccountLive>
        : TAccountLive,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type TipLiveInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  menuIndex: Option<number>;
};

export type TipLiveInstructionDataArgs = {
  amount: number | bigint;
  menuIndex: OptionOrNullable<number>;
};

export function getTipLiveInstructionDataEncoder(): Encoder<TipLiveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
      ["menuIndex", getOptionEncoder(getU8Encoder())],
    ]),
    (value) => ({ ...value, discriminator: TIP_LIVE_DISCRIMINATOR }),
  );
}

export function getTipLiveInstructionDataDecoder(): Decoder<TipLiveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
    ["menuIndex", getOptionDecoder(getU8Decoder())],
  ]);
}

export function getTipLiveInstructionDataCodec(): Codec<
  TipLiveInstructionDataArgs,
  TipLiveInstructionData
> {
  return combineCodec(
    getTipLiveInstructionDataEncoder(),
    getTipLiveInstructionDataDecoder(),
  );
}

export type TipLiveInput<
  TAccountFan extends string = string,
  TAccountLive extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  fan: TransactionSigner<TAccountFan>;
  live: Address<TAccountLive>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: TipLiveInstructionDataArgs["amount"];
  menuIndex: TipLiveInstructionDataArgs["menuIndex"];
};

export function getTipLiveInstruction<
  TAccountFan extends string,
  TAccountLive extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: TipLiveInput<TAccountFan, TAccountLive, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): TipLiveInstruction<
  TProgramAddress,
  TAccountFan,
  TAccountLive,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    fan: { value: input.fan ?? null, isWritable: true },
    live: { value: input.live ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.fan),
      getAccountMeta(accounts.live),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getTipLiveInstructionDataEncoder().encode(
      args as TipLiveInstructionDataArgs,
    ),
    programAddress,
  } as TipLiveInstruction<
    TProgramAddress,
    TAccountFan,
    TAccountLive,
    TAccountSystemProgram
  >);
}

export type ParsedTipLiveInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    fan: TAccountMetas[0];
    live: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: TipLiveInstructionData;
};

export function parseTipLiveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTipLiveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      fan: getNextAccount(),
      live: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getTipLiveInstructionDataDecoder().decode(instruction.data),
  };
}
