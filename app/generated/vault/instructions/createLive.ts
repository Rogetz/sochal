/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findGlobalPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getMenuItemDecoder,
  getMenuItemEncoder,
  type MenuItem,
  type MenuItemArgs,
} from "../types";

export const CREATE_LIVE_DISCRIMINATOR = new Uint8Array([
  222, 219, 61, 58, 183, 36, 101, 227,
]);

export function getCreateLiveDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_LIVE_DISCRIMINATOR);
}

export type CreateLiveInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountGlobal extends string | AccountMeta<string> = string,
  TAccountLive extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountGlobal extends string
        ? WritableAccount<TAccountGlobal>
        : TAccountGlobal,
      TAccountLive extends string
        ? WritableAccount<TAccountLive>
        : TAccountLive,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateLiveInstructionData = {
  discriminator: ReadonlyUint8Array;
  topic: string;
  menuItems: Array<MenuItem>;
};

export type CreateLiveInstructionDataArgs = {
  topic: string;
  menuItems: Array<MenuItemArgs>;
};

export function getCreateLiveInstructionDataEncoder(): Encoder<CreateLiveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["topic", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["menuItems", getArrayEncoder(getMenuItemEncoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_LIVE_DISCRIMINATOR }),
  );
}

export function getCreateLiveInstructionDataDecoder(): Decoder<CreateLiveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["topic", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["menuItems", getArrayDecoder(getMenuItemDecoder())],
  ]);
}

export function getCreateLiveInstructionDataCodec(): Codec<
  CreateLiveInstructionDataArgs,
  CreateLiveInstructionData
> {
  return combineCodec(
    getCreateLiveInstructionDataEncoder(),
    getCreateLiveInstructionDataDecoder(),
  );
}

export type CreateLiveAsyncInput<
  TAccountCreator extends string = string,
  TAccountGlobal extends string = string,
  TAccountLive extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  global?: Address<TAccountGlobal>;
  live: Address<TAccountLive>;
  systemProgram?: Address<TAccountSystemProgram>;
  topic: CreateLiveInstructionDataArgs["topic"];
  menuItems: CreateLiveInstructionDataArgs["menuItems"];
};

export async function getCreateLiveInstructionAsync<
  TAccountCreator extends string,
  TAccountGlobal extends string,
  TAccountLive extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CreateLiveAsyncInput<
    TAccountCreator,
    TAccountGlobal,
    TAccountLive,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateLiveInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGlobal,
    TAccountLive,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    global: { value: input.global ?? null, isWritable: true },
    live: { value: input.live ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.global.value) {
    accounts.global.value = await findGlobalPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.global),
      getAccountMeta(accounts.live),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateLiveInstructionDataEncoder().encode(
      args as CreateLiveInstructionDataArgs,
    ),
    programAddress,
  } as CreateLiveInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGlobal,
    TAccountLive,
    TAccountSystemProgram
  >);
}

export type CreateLiveInput<
  TAccountCreator extends string = string,
  TAccountGlobal extends string = string,
  TAccountLive extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  global: Address<TAccountGlobal>;
  live: Address<TAccountLive>;
  systemProgram?: Address<TAccountSystemProgram>;
  topic: CreateLiveInstructionDataArgs["topic"];
  menuItems: CreateLiveInstructionDataArgs["menuItems"];
};

export function getCreateLiveInstruction<
  TAccountCreator extends string,
  TAccountGlobal extends string,
  TAccountLive extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CreateLiveInput<
    TAccountCreator,
    TAccountGlobal,
    TAccountLive,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateLiveInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountGlobal,
  TAccountLive,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    global: { value: input.global ?? null, isWritable: true },
    live: { value: input.live ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.global),
      getAccountMeta(accounts.live),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateLiveInstructionDataEncoder().encode(
      args as CreateLiveInstructionDataArgs,
    ),
    programAddress,
  } as CreateLiveInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGlobal,
    TAccountLive,
    TAccountSystemProgram
  >);
}

export type ParsedCreateLiveInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    global: TAccountMetas[1];
    live: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateLiveInstructionData;
};

export function parseCreateLiveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateLiveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      global: getNextAccount(),
      live: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateLiveInstructionDataDecoder().decode(instruction.data),
  };
}
