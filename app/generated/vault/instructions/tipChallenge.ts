/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const TIP_CHALLENGE_DISCRIMINATOR = new Uint8Array([
  246, 203, 40, 27, 148, 250, 158, 36,
]);

export function getTipChallengeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TIP_CHALLENGE_DISCRIMINATOR,
  );
}

export type TipChallengeInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountFan extends string | AccountMeta<string> = string,
  TAccountChallenge extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFan extends string
        ? WritableSignerAccount<TAccountFan> & AccountSignerMeta<TAccountFan>
        : TAccountFan,
      TAccountChallenge extends string
        ? WritableAccount<TAccountChallenge>
        : TAccountChallenge,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type TipChallengeInstructionData = {
  discriminator: ReadonlyUint8Array;
  creatorSide: number;
  amount: bigint;
};

export type TipChallengeInstructionDataArgs = {
  creatorSide: number;
  amount: number | bigint;
};

export function getTipChallengeInstructionDataEncoder(): FixedSizeEncoder<TipChallengeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["creatorSide", getU8Encoder()],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TIP_CHALLENGE_DISCRIMINATOR }),
  );
}

export function getTipChallengeInstructionDataDecoder(): FixedSizeDecoder<TipChallengeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["creatorSide", getU8Decoder()],
    ["amount", getU64Decoder()],
  ]);
}

export function getTipChallengeInstructionDataCodec(): FixedSizeCodec<
  TipChallengeInstructionDataArgs,
  TipChallengeInstructionData
> {
  return combineCodec(
    getTipChallengeInstructionDataEncoder(),
    getTipChallengeInstructionDataDecoder(),
  );
}

export type TipChallengeInput<
  TAccountFan extends string = string,
  TAccountChallenge extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  fan: TransactionSigner<TAccountFan>;
  challenge: Address<TAccountChallenge>;
  systemProgram?: Address<TAccountSystemProgram>;
  creatorSide: TipChallengeInstructionDataArgs["creatorSide"];
  amount: TipChallengeInstructionDataArgs["amount"];
};

export function getTipChallengeInstruction<
  TAccountFan extends string,
  TAccountChallenge extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: TipChallengeInput<
    TAccountFan,
    TAccountChallenge,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): TipChallengeInstruction<
  TProgramAddress,
  TAccountFan,
  TAccountChallenge,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    fan: { value: input.fan ?? null, isWritable: true },
    challenge: { value: input.challenge ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.fan),
      getAccountMeta(accounts.challenge),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getTipChallengeInstructionDataEncoder().encode(
      args as TipChallengeInstructionDataArgs,
    ),
    programAddress,
  } as TipChallengeInstruction<
    TProgramAddress,
    TAccountFan,
    TAccountChallenge,
    TAccountSystemProgram
  >);
}

export type ParsedTipChallengeInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    fan: TAccountMetas[0];
    challenge: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: TipChallengeInstructionData;
};

export function parseTipChallengeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTipChallengeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      fan: getNextAccount(),
      challenge: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getTipChallengeInstructionDataDecoder().decode(instruction.data),
  };
}
