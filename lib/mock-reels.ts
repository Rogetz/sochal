// Intentionally empty — Sochal starts with no streams.
// Real streams come from the user via sochal.startStream() (and, eventually,
// from an on-chain indexer subscribed to the Anchor program's `Live` accounts).
import type { Reel } from "@/components/sochal/ReelCard";
export const MOCK_REELS: Reel[] = [];
